import template from './pets.html';
// import styles from './pets.scss';

export default {
  template,
  controller,
  bindings: {
    stores: '<'
  }
};

controller.$inject = [];

function controller() {

  // this.styles = styles;
}